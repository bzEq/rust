name: AIX build
"on":
  push:
    branches:
      - aix-build-test
jobs:
  build-on-aix:
    runs-on: ubuntu-latest
    steps:
    - name: Install dependencies
      run: |
        sudo apt install curl
    - name: Build remotely
      run: src/ci/scripts/run-build-on-aix.py
